services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=models/model.pth
      # Galería de test (opcional): descarga el dataset de Kaggle y establece
      # la variable de entorno TEST_IMAGES_PATH apuntando a la carpeta /test
      # del dataset, luego monta el volumen correspondiente. Ejemplo:
      #   TEST_IMAGES_PATH=/data/test
      # y añade en volumes:
      #   - /ruta/local/chest_xray/test:/data/test:ro
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v2.52.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    depends_on:
      - api
    restart: unless-stopped

  # Los servicios node-exporter y cadvisor montan rutas del kernel Linux
  # (/proc, /sys, /var/run/docker.sock) y no funcionan en Windows.
  # Se incluyen en el perfil "infra" para activarlos solo en Linux/macOS:
  #   docker-compose up --build --profile infra
  node-exporter:
    image: prom/node-exporter:latest
    profiles: ["infra"]
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    restart: unless-stopped

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    profiles: ["infra"]
    ports:
      - "8081:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
    command:
      - '--store_container_labels=true'
      - '--whitelisted_container_labels=com.docker.compose.service'
    privileged: true
    restart: unless-stopped

  grafana:
    image: grafana/grafana:10.4.2
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:
